{{>include "./SliderBase.hbs"}}

{{#*inline "handles"}}
	<div class="ui5-slider-handle" style="{{styles.handlePosition}}">
		{{#if showTooltip}}
			<div class="ui5-slider-tooltip" style="{{styles.tooltipVisibility}}">
				<span class="ui5-slider-tooltip-value">{{tooltipValue}}</span>
			</div>
		{{/if}}
	</div>
{{/inline}}