<span>
{{#if _renderEmptyIndicator}}
	<span class="empty-indicator" aria-hidden="true">{{_emptyIndicatorSymbol}}</span>
	<span class="empty-indicator-aria-label">{{_emptyIndicatorAriaLabel}}</span>
{{else}}
	<slot></slot>
{{/if}}
</span>