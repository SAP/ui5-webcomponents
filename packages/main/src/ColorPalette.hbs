<div class="ui5-cp-swatch-container" 
	role="region"
	aria-label="{{colorContainerLabel}}">
	{{#each entries}}
		<slot
			name="{{this.item._individualSlot}}"
			@click={{../_onclick}}
			@keyup={{../_onkeyup}}
			index="{{index}}">
		</slot>
	{{/each}}
</div>