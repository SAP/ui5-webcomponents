class TabSeparatorTemplateContext {
	static calculate(state) {
		const context = {
			ctr: state,
			classes: {
				main: {},
			},
		};

		return context;
	}
}

export default TabSeparatorTemplateContext;
