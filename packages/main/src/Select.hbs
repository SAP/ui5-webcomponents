<div
	class="ui5-select-root"
	tabindex="{{tabIndex}}"
	dir="{{dir}}"
	@keydown="{{_onkeydown}}"
	@keyup="{{_onkeyup}}"
	@focusin="{{_onfocusin}}"
	@focusout="{{_onfocusout}}"
>
	<div
		class="ui5-select-label-root"
		@click="{{_togglePopover}}">
		<ui5-label content-density="{{effectiveContentDensity}}">{{_text}}</ui5-label>
	</div>

	{{#if options}}
		<ui5-popover
			id="ui5-select--popover"
			content-density="{{effectiveContentDensity}}"
			placement-type="Bottom"
			no-arrow
			horizontal-align="Stretch"
			@ui5-afterOpen="{{_applyFocusAfterOpen}}"
			@ui5-beforeOpen="{{_beforeOpen}}"
			@ui5-afterClose="{{_afterClose}}"
		>
				<ui5-list mode="SingleSelect" separators="None" @keydown="{{_handlePickerKeydown}}" @ui5-itemPress="{{_selectionChange}}" content-density="{{effectiveContentDensity}}">
					{{#each _syncedOptions}}
						<ui5-li ?selected="{{this.selected}}" icon="{{this.icon}}" id="{{this.id}}-li" content-density="{{effectiveContentDensity}}">
							{{this.textContent}}
						</ui5-li>
					{{/each}}
				</ui5-list>
		</ui5-popover>
	{{/if}}

		<ui5-icon 
			@click="{{_togglePopover}}"
			name="slim-arrow-down"
			input-icon
			content-density="{{effectiveContentDensity}}"
			?pressed="{{_iconPressed}}"
			dir="{{dir}}"
		></ui5-icon>

	<slot name="formSupport"></slot>
</div>
