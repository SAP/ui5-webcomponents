<div tabindex="{{_tabIndex}}" @click="{{_select}}" @keydown="{{_keydown}}" class="ui5-token--wrapper">
	<span class="ui5-token--text"><slot></slot></span>

	{{#unless readonly}}
		<ui5-icon-svg @click="{{_delete}}" src="{{iconURI}}" class="ui5-token--icon"></ui5-icon-svg>
	{{/unless}}
</div>
