<div class="ui5-toast-root-parent"
     dir="{{rtl}}">
	<div class="ui5-toast-root"
	     placement="{{placement}}"
	     style="{{styles.root}}"
		 @transitionend="{{_ontransitionend}}">
		<span class="ui5-toast-text">
			<bdi>
				<slot></slot>
			</bdi>
		</span>
	</div>
</div>