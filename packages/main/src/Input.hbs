<div {{> controlData}}
	class="{{classes.main}}"
	style="{{styles.main}}"
	aria-invalid="{{ariaInvalid}}"
	aria-labelledBy="{{ariaLabelledBy}}"
>
<div id="{{ctr._id}}-wrapper"
	class="{{classes.wrapper}}"
>
	<input id="{{ctr._id}}-inner"
			class="sapWCInputBaseInner"
			type="{{type}}"
			?disabled="{{ctr.disabled}}"
			?readonly="{{_readonly}}"
			.value="{{ctr.value}}"
			placeholder="{{ctr.placeholder}}"
			@input="{{ctr._input.onInput}}"
			@change="{{ctr._input.change}}"
			data-sap-no-tab-ref
			data-sap-focus-ref
	/>
		{{#if ctr.icon}}
			<slot name="icon"></slot>
		{{/if}}
	</div>

	{{#if ctr.showSuggestions}}
			<ui5-popover
				placement-type="Bottom"
				hide-header="true"
				hide-arrow="true"
				horizontal-align="Stretch"
				initial-focus="{{ctr._id}}-inner">
					<ui5-list separators="Inner">
                        <slot></slot>
					</ui5-list>
			</ui5-popover>
	{{/if}}

	<slot name="formSupport"></slot>
</div>
