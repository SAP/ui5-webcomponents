<label
		class="ui5-label-root"
		dir="{{effectiveDir}}"
		@click={{_onclick}}
		for="{{for}}"
	>
	<span class="ui5-label-text-wrapper">
		<bdi id="{{_id}}-bdi">
			<slot></slot>
		</bdi>
	</span>
	<span class="ui5-label-required-colon"></span>
</label>
