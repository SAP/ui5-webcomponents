<label
		class="ui5-label-root"
		dir="{{effectiveDir}}"
		@click={{_onclick}}
		for="{{for}}"
	>
	<span class="{{classes.textWrapper}}">
		<bdi id="{{_id}}-bdi">
			<slot></slot>
		</bdi>
	</span>
	<span class="ui5-label-required-colon"></span>
</label>
