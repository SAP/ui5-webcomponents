{{#if _popin}}
	{{#if _popinText}}
		{{_popinText}}
		<span class="popin-colon">{{_i18nPopinColon}}</span>
	{{else if _popinHeader}}
		{{_popinHeader}}
		<span class="popin-colon">{{_i18nPopinColon}}</span>
	{{/if}}
	<slot></slot>
{{else}}
	<slot></slot>
{{/if}}