<ui5-button
	class="ui5-otb-button"
	style="{{style}}"
	icon="{{this.icon}}"
	design="{{this.design}}"
	?disabled="{{this.disabled}}"
	@click="{{this.press}}"
	data-ui5-external-action-item-id="{{this.refItemid}}"
	.refItemId="{{this.refItemid}}"
	?always-overflow="{{alwaysOverflow}}"
>
	{{#if this.imageSrc}}
		<div class="ui5-otb-action-content">
			{{#if this.imageLast}}
				<div class="ui5-otb-action-content-text ui5-otb-action-content-image-last">{{this.text}}</div>
			{{/if}}

			<img src="{{this.imageSrc}}" alt="{{imageAlt}}" width="{{imageWidth}}" height="{{imageHeight}}">

			{{#unless this.imageLast}}
				<div class="ui5-otb-action-content-text">{{this.text}}</div>
			{{/unless}}
		</div>
	{{else}}
		{{this.text}}
	{{/if}}
</ui5-button>