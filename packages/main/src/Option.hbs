<ui5-li-custom
	id="{{id}}-li"
	icon="{{icon}}"
	?selected="{{selected}}"
	?focused="{{focused}}"
	title="{{title}}"
	additional-text="{{additionalText}}"
	?aria-selected="{{selected}}"
	data-ui5-stable="{{stableDomRef}}"
>
	<slot></slot>
</ui5-li-custom>