<button
		type="button"
		class="{{classes.main}}"
		?disabled="{{disabled}}"
		aria-disabled="{{ariaDisabled}}"
		data-sap-focus-ref
		{{> ariaPressed}}
	>
		{{#if icon}}
			<ui5-icon
				class="{{classes.icon}}"
				src="{{icon}}"
			></ui5-icon>
		{{/if}}

		{{#if textContent}}
			<span id="{{_id}}-content" class="{{classes.text}}">
				<bdi>
					<slot></slot>
				</bdi>
			</span>
		{{/if}}
</button>

{{#*inline "ariaPressed"}}{{/inline}}
