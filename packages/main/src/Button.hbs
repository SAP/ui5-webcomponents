<button
		type="button"
		class="{{classes.main}}"
		?disabled="{{disabled}}"
		data-sap-focus-ref
		{{> ariaPressed}}
		dir="{{rtl}}"
		@focusout={{onfocusout}}
	>
		{{#if icon}}
			<ui5-icon
				class="{{classes.icon}}"
				src="{{icon}}"
			></ui5-icon>
		{{/if}}

		{{#if textContent}}
			<span id="{{_id}}-content" class="{{classes.text}}">
				<bdi>
					<slot></slot>
				</bdi>
			</span>
		{{/if}}
</button>

{{#*inline "ariaPressed"}}{{/inline}}
