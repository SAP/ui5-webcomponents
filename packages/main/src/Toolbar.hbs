<div
	class="ui5-tb-root"
	tabindex="{{tabIndex}}"
	part="otb"
>
	{{#if hasStartContent}}
		<div class="ui5-tb-start">
			<slot name="startContent"></slot>
		</div>
	{{/if}}

	<div class="{{classes.actions}}" @click="{{onCustomActionClick}}">
		{{#each standardActions}}
			{{this.toolbarTemplate}}
		{{/each}}

		{{#if showOverflowBtn}}
			<ui5-button
				icon="overflow"
				design="Transparent"
				?disabled="{{this.disabled}}"
				@click="{{onBtnOverflowClick}}"
				class="ui5-tb-overflow-btn"
			></ui5-button>
		{{/if}}
	</div>
</div>