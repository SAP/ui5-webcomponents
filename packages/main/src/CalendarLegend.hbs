<div class="ui5-calendar-legend-root">
	{{#each defaultItemsMapping}}
		{{#unless this.hide}}
			<ui5-calendar-legend-item 
				type={{this.type}}
				@focusin="{{../_onFocusIn}}"
				@keydown="{{../_onItemKeyDown}}"
				@mousedown="{{_onMouseDown}}"
			>
			</ui5-calendar-legend-item>
		{{/unless}}
	{{/each}}

	{{#each legendItems}}
		<div
			@focusin="{{../_onFocusIn}}"
			@keydown="{{../_onItemKeyDown}}"
			@mousedown="{{../_onMouseDown}}"
		>
			<slot name="{{this._individualSlot}}"></slot>
		</div>
	{{/each}}
</div>