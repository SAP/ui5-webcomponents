<div class="ui5-calendar-legend-root">
	{{#each typeFromEnum}}
		{{#unless this.hide}}
			<ui5-calendar-legend-item 
				type={{this.type}}
				@click="{{../_onItemClick}}"
			>
			</ui5-calendar-legend-item>
		{{/unless}}
	{{/each}}
</div>