<div class="{{classes.main}}"
	id="{{_id}}"
	role="alert"
	aria-live="assertive"
	aria-labelledby="{{_id}}">

	{{#unless noIcon}}
		<ui5-icon content-density="{{effectiveContentDensity}}" class="ui5-messagestrip-icon" name="{{messageStripIcon}}"></ui5-icon>
	{{/unless}}

	<span class="ui5-hidden-text">{{hiddenText}}</span>
	<span class="{{classes.label}}"><slot></slot></span>

	{{#unless noCloseButton}}
		<button
			class="ui5-messagestrip-close-icon-wrapper"
			title="{{_closeButtonText}}"
			@click={{_closeClick}}
		>
			<ui5-icon
				content-density="{{effectiveContentDensity}}"
				class="ui5-messagestrip-close-icon"
				name="decline"
			>
			</ui5-icon>
		</button>
	{{/unless}}
</div>
