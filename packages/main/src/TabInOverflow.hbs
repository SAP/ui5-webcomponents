<ui5-li-custom
	id="{{this._id}}"
	class="{{this.overflowClasses}}"
	type="{{this.overflowState}}"
	?selected="{{this.effectiveSelected}}"
	?disabled="{{this.effectiveDisabled}}"
>
	<div class="ui5-tc__overflowItemContent">
		{{#if this.icon}}
			<ui5-icon name="{{this.icon}}"></ui5-icon>
		{{/if}}

		{{this.text}}

		{{#if this.additionalText}}
			({{this.additionalText}})
		{{/if}}
	</div>
</ui5-li-custom>
