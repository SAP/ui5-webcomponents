<a
	class="{{classes.main}}"
	role="link"
	href="{{href}}"
	target="{{target}}"
	rel="{{_rel}}"
	tabindex="{{tabIndex}}"
	?disabled="{{disabled}}"
	?aria-disabled="{{ariaDisabled}}">
	<slot></slot>
</a>
