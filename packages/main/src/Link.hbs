<a
	class="ui5-link-root"
	role="link"
	href="{{parsedRef}}"
	target="{{target}}"
	rel="{{_rel}}"
	tabindex="{{tabIndex}}"
	?disabled="{{disabled}}"
	aria-disabled="{{ariaDisabled}}">
	<slot></slot>
</a>
