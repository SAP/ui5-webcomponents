const ToolbarPriority = {

	/**
	 * @public
	 */
	 Default: "Default",
	/**
	 * When set, the button will be always part of the overflow part of the toolbar.

	 * @public
	 */
	Never: "Never",

	/**
	 * @public
	 */
	Always: "Always",
};

export default ToolbarPriority;
