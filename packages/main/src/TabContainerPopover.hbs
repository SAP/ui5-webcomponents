<ui5-responsive-popover
	id="{{_id}}-overflowMenu"
	horizontal-align="Right"
	placement-type="Bottom"
	content-only-on-desktop
	hide-arrow
	_hide-header
	class="ui5-tab-container-responsive-popover"
>
	<ui5-list
		_max-nesting-level="{{maxNestingLevel}}"
		mode="SingleSelect"
		separators="None"
		@ui5-item-click="{{_onOverflowListItemClick}}"
		@ui5-item-reorder="{{_onReorderItemsInPopover}}"
		@ui5-before-item-reorder="{{_onBeforeReorderItemsInPopover}}"
	>
		{{#each _popoverItems}}
			{{overflowPresentation}}
		{{/each}}
	</ui5-list>
	<div slot="footer" class="ui5-responsive-popover-footer">
		<ui5-button
			design="Transparent"
			@click="{{_closeRespPopover}}"
		>{{popoverCancelButtonText}}</ui5-button>
	</div>
</ui5-responsive-popover>