<ui5-responsive-popover
	id="{{_id}}-overflowMenu"
	horizontal-align="Right"
	placement-type="Bottom"
	content-only-on-desktop
	hide-arrow
	_hide-header
	class="ui5-tab-container-responsive-popover"
>
	<ui5-list mode="SingleSelect" separators="None" @ui5-item-click="{{_onOverflowListItemClick}}">
		{{#if _endOverflowItems.length}}
			{{#each _endOverflowItems}}
				{{this.overflowPresentation}}
			{{/each}}
		{{else}}
			{{#each _startOverflowItems}}
				{{this.overflowPresentation}}
			{{/each}}
		{{/if}}
	</ui5-list>
	<div slot="footer" class="ui5-responsive-popover-footer">
		<ui5-button
			design="Transparent"
			@click="{{_closeRespPopover}}"
		>Cancel</ui5-button>
	</div>
</ui5-responsive-popover>

{{!-- todo reuse the above popover for expand button click purposes --}}

<ui5-responsive-popover id="{{_id}}-expandButtonPopover" horizontal-align="Right" placement-type="Bottom"
	content-only-on-desktop hide-arrow _hide-header class="ui5-tab-container-responsive-popover">
	<ui5-list mode="SingleSelect" separators="None" @ui5-item-click="{{_onOverflowListItemClick}}">
				{{#each _tabItems}}
					<ui5-li-custom id="{{this._id}}" class="{{classes}}" type="{{this.overflowState}}"
						?disabled="{{this.effectiveDisabled}}" aria-disabled="{{this.effectiveDisabled}}"
						aria-selected="{{this.effectiveSelected}}" aria-labelledby="{{this.ariaLabelledBy}}">
						<div class="ui5-tab-overflow-itemContent" style="{{style}}">
							{{#if item.icon}}
							<ui5-icon name="{{item.icon}}"></ui5-icon>
							{{/if}}
							{{item.text}}
							{{#if item.additionalText}}
							({{item.additionalText}})
							{{/if}}
						</div>
					</ui5-li-custom>
			{{/each}}
	</ui5-list>
	<div slot="footer" class="ui5-responsive-popover-footer">
		<ui5-button design="Transparent" @click="{{_closeRespPopover}}">Cancel</ui5-button>
	</div>
</ui5-responsive-popover>
