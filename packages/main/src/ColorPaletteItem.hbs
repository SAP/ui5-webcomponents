<div
	class="ui5-cp-item"
	style="{{styles.root}}"
	value="{{value}}"
	tabindex="{{_tabIndex}}"
	role="button"
	aria-label="{{colorLabel}} - {{index}}: {{this.value}}"
	title="{{colorLabel}} - {{index}}: {{this.value}}"
	?disabled="{{_disabled}}"
>
</div>
