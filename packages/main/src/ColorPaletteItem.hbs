<div
	class="ui5-cp-item"
	style="background-color: {{value}}"
	value="{{value}}"
	tabindex="{{_tabIndex}}"
	role="button"
	aria-label="{{colorLabel}} - {{index}}: {{this.value}}"
	title="{{colorLabel}} - {{index}}: {{this.value}}"
	?disabled="{{_disabled}}"
>
</div>