<div
	class="{{classes.root}}"
	style="{{styles.root}}"
	value="{{value}}"
	tabindex="{{forcedTabIndex}}"
	role="button"
	aria-label="{{colorLabel}} - {{index}}: {{this.value}}"
	title="{{colorLabel}} - {{index}}: {{this.value}}"
	?disabled="{{_disabled}}"
>
</div>
