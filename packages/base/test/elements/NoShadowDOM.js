import UI5Element from "../../UI5Element.js";

const metadata = {
	tag: "ui5-test-no-shadow",
};

class NoShadow extends UI5Element {
	static get metadata() {
		return metadata;
	}
}

NoShadow.define();
