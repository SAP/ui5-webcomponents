<div class="ui5-sn-root" role="navigation">
	{{> header }}

	{{#if items.length}}
		<ui5-tree
			id="ui5-sn-items-tree"
			class="ui5-sn-items-tree"
			mode="None"
			.accessibleRoleDescription="{{ariaRoleDescNavigationList}}"
			?_minimal="{{collapsed}}"
			_toggle-button-end
			@ui5-item-click="{{handleTreeItemClick}}"
		>
			{{#each _items}}
				<ui5-tree-item
					icon="{{this.item.icon}}"
					.associatedItem="{{this.item}}"
					text="{{this.item.text}}"
					title="{{this.item._tooltip}}"
					?has-children="{{this.item.items.length}}"
					?expanded="{{this.item.expanded}}"
					?selected="{{this.selected}}"
					.ariaHaspopup="{{this.item._ariaHasPopup}}"
					.accessibleRoleDescription="{{../ariaRoleDescNavigationListItem}}"
					._fixed="{{this.item._fixed}}"
				>
					{{#unless ../collapsed}}
						{{#each this.item.items}}
							<ui5-tree-item
								.associatedItem="{{this}}"
								text="{{this.text}}"
								title="{{this._tooltip}}"
								icon="{{this.icon}}"
								?selected="{{this.selected}}"
								.accessibleRoleDescription="{{../ariaRoleDescNavigationListItem}}"
							>
							</ui5-tree-item>
						{{/each}}
					{{/unless}}
				</ui5-tree-item>
			{{/each}}
		</ui5-tree>
	{{/if}}

	<div class="ui5-sn-spacer"></div>

	{{#if fixedItems.length}}
		<div>
			<div class="ui5-sn-bottom-content-border">
				<span></span>
			</div>
			<ui5-tree
				id="ui5-sn-fixed-items-tree"
				mode="None"
				?_minimal="{{collapsed}}"
				_toggle-button-end
				@ui5-item-click="{{handleTreeItemClick}}"
				.accessibleRoleDescription="{{ariaRoleDescNavigationList}}"
			>
				{{#each _fixedItems}}
					<ui5-tree-item
						icon="{{this.item.icon}}"
						.associatedItem="{{this.item}}"
						text="{{this.item.text}}"
						title="{{this.item._tooltip}}"
						?has-children="{{this.item.items.length}}"
						?expanded="{{this.item.expanded}}"
						?selected="{{this.selected}}"
						.ariaHaspopup="{{this.item._ariaHasPopup}}"
						.accessibleRoleDescription="{{../ariaRoleDescNavigationListItem}}"
					>
						{{#unless ../collapsed}}
							{{#each this.item.items}}
								<ui5-tree-item
									.associatedItem="{{this}}"
									text="{{this.text}}"
									title="{{this._tooltip}}"
									?selected="{{this.selected}}"
									.accessibleRoleDescription="{{../ariaRoleDescNavigationListItem}}"
								>
								</ui5-tree-item>
							{{/each}}
						{{/unless}}
					</ui5-tree-item>
				{{/each}}
			</ui5-tree>
		</div>
	{{/if}}

	{{> footer }}
</div>

{{#*inline header}}
	{{#if showHeader}}
		<slot name="header"></slot>
	{{/if}}
{{/inline}}

{{#*inline footer}}{{/inline}}
