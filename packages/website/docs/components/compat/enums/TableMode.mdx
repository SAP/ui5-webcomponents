Different table modes.

## Enum fields
| Name | Description |
|------|-------------|
| None | Default mode (no selection). |
| SingleSelect | Single selection mode (only one table row can be selected). |
| MultiSelect | Multi selection mode (more than one table row can be selected). |